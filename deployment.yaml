apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app: calcom
  name: calcom
  namespace: calcom
spec:
  replicas: 1
  selector:
    matchLabels:
      app: calcom
  strategy:
    type: RollingUpdate
  template:
    metadata:
      labels:
        app: calcom
    spec:
      containers:
        - image: calcom/cal.com:latest
          name: calcom
          ports:
            - containerPort: 3000
          envFrom:
          - secretRef:
              name: calcom-secret
          env:
          - name: DATABASE_URL
            valueFrom:
              secretKeyRef:
                name: calcom-postgresql-app
                key: uri
          - name: DATABASE_DIRECT_URL
            valueFrom:
              secretKeyRef:
                name: calcom-postgresql-app
                key: uri
          - name: NEXT_PUBLIC_WEBAPP_URL
            value: https://cal.ryzen.me
          - name: NEXT_PUBLIC_WEBSITE_URL
            value: https://cal.ryzen.me
          - name: NEXT_PUBLIC_EMBED_LIB_URL
            value: https://cal.ryzen.me/embed/embed.js
      restartPolicy: Always